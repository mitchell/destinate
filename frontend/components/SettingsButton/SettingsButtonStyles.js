import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    paddingVertical: 20,
    width: '100%',
    borderColor: '#DDDDDD',
    borderWidth: 0.5,
    backgroundColor: '#fff',
    alignItems: 'center',
  },
});
